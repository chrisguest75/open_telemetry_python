## Install
To install locally you can clone the repo

```sh
export PIPENV_VENV_IN_PROJECT=1
pipenv install --three
pipenv shell
```




## Create from scratch


```sh
export PIPENV_VENV_IN_PROJECT=1
pipenv install --three
git init
```